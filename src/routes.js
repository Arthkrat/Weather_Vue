/* import Vue from 'vue';
import VueRouter from 'vue-router';

Vue.use(VueRouter);

import { store } from './store';

const routes= [
    {
        
    }
];
export const router = new VueRouter({
    routes,
    mode: 'history'
})*/
